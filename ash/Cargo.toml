# Plan 330: ash CLI/TUI workspace.
# Keep the CLI/TUI build independent of AutoLang's default UI feature. Its
# auto-lang dependencies disable defaults and retain file-history support;
# ash-gui lives in ../ash-gui and explicitly enables ui-iced in its workspace.
#
# Plan 037 M2.2: the workspace now has three members:
#   - auto-shell : pure Shell logic + commands (zero terminal deps)
#   - ash-tui    : terminal frontend (reedline/crossterm/ratatui/nu-ansi-term)
#   - ash        : the CLI binary (composition root — wires shell + tui)
[workspace]
members = ["auto-shell", "ash"]
resolver = "2"

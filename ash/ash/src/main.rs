// Plan 037 M2.2 batch 0: skeleton placeholder. The real main.rs (with CLI arg
// parsing, REPL launch, hook injection) moves here in batch 4. For now this
// just proves the dependency graph (auto-shell + ash-tui + ash-core) compiles
// without cycles.
fn main() {
    eprintln!("ash: skeleton (Plan 037 M2.2 batch 0) — main.rs not yet wired");
    std::process::exit(1);
}

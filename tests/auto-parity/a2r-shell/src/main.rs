//! a2r-shell — auto-parity 的 a2r 通道最小壳(Plan 080 L0)。
//!
//! 用例 bin 由 run.py 经 `auto trans --path <case>.at rust` 产出到
//! `src/bin/<case>.rs`(gitignore,构建再生)后 `cargo run --release --bin`。
//! 本 main 仅占位说明,不参与比对。

fn main() {
    eprintln!("a2r-shell: 占位 bin;用例 bin 由 tests/auto-parity/run.py 生成于 src/bin/");
}

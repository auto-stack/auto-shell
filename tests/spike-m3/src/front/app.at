use back.api: history

widget App {
    model {
        var count str = "boot"
    }
    on {
        .Init -> {
            var h = history()
            var n int = h.len()
            .count = "len="
            .count = .count + n.str()
        }
    }
    view {
        col {
            style: "gap-2 p-4"
            text .count {
                style: "text-fg text-lg"
            }
        }
    }
}

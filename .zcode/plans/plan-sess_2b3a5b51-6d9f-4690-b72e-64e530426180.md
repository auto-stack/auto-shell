## 任务概览

把顶层 `plans/`（30 个 .md）移动到 `docs/plans/`，同步更新散落的路径引用，逐一更新计划状态字段（修正 3 处过时状态），最后产出完成情况总结。

## 第一部分：移动文件 + 更新引用

### 1. 创建 `docs/plans/` 并移动 30 个文件
用 `git mv` 保留历史（已确认 30 个文件都在 git 跟踪中）：
```
mkdir -p docs/plans
git mv plans/*.md docs/plans/
```
移动后顶层 `plans/` 空目录删除。

### 2. 更新 21 处路径引用（分布在 13 个文件）
全部把 `plans/` → `docs/plans/`（根相对）；1 处相对目录引用 `../plans/` → `../docs/plans/`：

**源码注释（3 处）：**
- `ash/auto-shell/src/frontend/ai.rs:8`
- `ash-core/src/cmd/value_helpers.rs:259`
- `ash-core/src/security.rs:16`

**文档（18 处）：**
- `README.md:213,235`（目录树 + 链接）
- `docs/roadmap.md:285`
- `ash/auto-shell/tests/parity/README.md:6,22,35,50,58`（5 处）
- `designs/030-ash-gui.md:1041`
- `designs/031-data-processing.md:531`
- `designs/032-intelligent-completion.md:403`
- `designs/033-plugin-ecosystem.md:78,436`
- `designs/036-script-parity.md:5`（`../plans/` → `../docs/plans/`，相对目录）

**移动后 plan 文件自引用（2 处，路径前缀同步）：**
- `docs/plans/027-ash-ai-chat-mode.md`（原 `plans/027:325`）
- `docs/plans/030-ash-gui.md`（原 `plans/030:1665`）

### 不做的事
- `designs/` 不移动（指令只说 plans/）
- `.zcode/plans/` 不动（工具会话目录）
- `docs/release_notes/` 的引用不动
- `designs/036:209` 的 superpowers 死链、`plans/013:525-527` 的 old/ 死链（既有问题，与本次移动无关，标注但不改）

## 第二部分：更新计划状态（3 处过时）

### A. `docs/plans/027-ash-ai-chat-mode.md`
头部 `Status: Design + Implementation Plan` → `Status: ✅ Done (v1 chat-only 全部实现)`。核查证据：`frontend/ai.rs` 333 行完整实现 ChatSession + 流式输出 + F4 接入 REPL，计划文末验收清单第 4 步也明确要求此更新。

### B. `docs/plans/036-script-parity.md` 正文表格过时
头部状态正确，但 **正文 Phase 4（约 line 344-375）与后文矛盾**：
- 表格 line 346/352：case 74/80 标 `skip` → 改为 `✅ 通过`
- 验收 line 354："77 实跑 + 2 skip" → "79/79 实跑 + 0 skip"
- 缺陷段落 line 364-375："新发现并钉住的缺陷(2 个 skip)" → 标注为已过时，删除或改为指向后文"Phase 4 后续:缺陷修复"（后文已记录 4 缺陷全部修复）

（后文 line 388-426 的"Phase 4 后续"小节是准确的，保留不动。）

### C. `ash/auto-shell/tests/parity/README.md` 验收段
同步修正过时表述"79/79 通过(77 实跑 + 2 skip)" → "79/79 全过(0 skip)"。

## 第三部分：完成情况总结（核查已完成的结论）

基于对 30 个计划的代码核查，分类如下（将在最终回复中给出带证据的总结）：

| 类别 | 计划 | 数量 |
|------|------|------|
| **完全完成** | 001-012, 015, 016, 018, 019, 021, 022, 023, 025, 026, 027, 036 | 23 |
| **核心完成 + 明确 deferred 项** | 013（Phase3-4 推迟）、014（设计阶段）、020（Phase6+附录A）、024（Tier2 deferred） | 4 |
| **几乎未启动** | **030（ash-gui，M0-M5 全未实现，仅 scaffold）** | 1 |
| **roadmap 类** | 017, 019, 020, roadmap-029-035 | (重叠) |

**workaround 情况**：仅 036 历史上有"5 类规避"，但已**全部转正为真实实现**（commits 03dc5f7/d23f091/24d7509/e08b505），当前无 workaround 完成项。

## 验证
- `git status` 确认移动 + 改动符合预期
- `grep -rn "plans/" --include=*.md --include=*.rs` 复查无遗漏的顶层 plans/ 引用（排除 .zcode、docs/plans、release_notes）
- 确认 docs/plans/ 下有 30 个文件

## 交付
1. 30 个计划文件迁移到 `docs/plans/`
2. 21 处引用更新
3. 3 处过时状态修正
4. 完成情况总结报告
5. 一次性 commit（或按你偏好分提交）
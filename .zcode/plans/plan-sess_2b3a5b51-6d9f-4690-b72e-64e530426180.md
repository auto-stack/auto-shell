## 上下文感知：Shell 访问器 + context builder + F4/F3 接入（§2.3 + §7.2）

### 范围（路 A 已确认）
1. Shell 加 4 个 pub 访问器（§7.2）
2. ai_context.rs 的 build_context_block（§2.3）
3. auto-ai 的 Agent 加 set_context setter（跨仓库小改）
4. F4 每轮注入 context、F3 system prompt 加 context

### A. auto-ai 侧（跨仓库小改）：Agent::set_context
`auto-ai/crates/auto-ai-agent/src/agent.rs` 加一个 setter：
```rust
/// Update the context block at runtime (cwd/last-command change between turns).
pub fn set_context(&mut self, context: impl Into<String>) {
    self.context_block = Some(context.into());
}
```
（context_block 字段已有 line 154，build_request 已读它 line 563，只差一个 mut setter。）

### B. auto-shell 侧：Shell 访问器（§7.2）
`shell.rs` 加 4 个 pub 方法（只读访问器，对应已有私有字段 line 117-128）：
- `pub fn last_command_line(&self) -> Option<&str>`（last_command_line: Option<String>）
- `pub fn last_command_args(&self) -> &[String]`
- `pub fn aliases(&self) -> &HashMap<String, String>`
- `pub fn dir_stack(&self) -> &[PathBuf]`

### C. auto-shell 侧：ai_context.rs（新文件，§2.3）
`frontend/ai_context.rs`：
```rust
pub fn build_context_block(shell: &Shell) -> String {
    // L0 静态：OS + cwd
    // L1 会话：上一条命令 + exit code
    // L2 别名：前 5 个
    // 不含 L3 输出（v1 不做）
}
```
照搬 §7.2 设计（line 876-890），用新增的访问器。

### D. F4 接入（frontend/ai.rs）
ChatSession 加 `update_context(&mut self, shell: &Shell)` 方法：调 build_context_block → agent.set_context。
repl.rs 的 handle_chat_turn 在 send_turn_streaming 前调 `session.update_context(&self.shell)`。
（ChatSession 已持有 agent，set_context 通过它。）

### E. F3 接入（repl.rs ask_ai）
ask_ai 的 system prompt（line 355）加入 build_context_block(&self.shell) 的内容。
（F3 每轮重建 system prompt，直接拼接即可。）

### 测试（TDD）
- **访问器**：set last_command_line/aliases → 读回正确
- **build_context_block**：含 cwd/OS/last command/aliases；空 aliases 时不输出别名行
- **Agent.set_context**（auto-ai）：set 后 build_request 注入（已有 build_request_injects_context_block 测试模式）
- F4/F3 回归不受影响

### 验证
- auto-ai：cargo test -p auto-ai-agent --lib（set_context 测试）
- auto-shell：cargo test -p auto-shell --lib frontend::ai + smart_command + ash_command_tool
- 回归全过

### 不做
- L3 输出留存（v1 明确排除）
- Warp 式建议（§7.3，独立大项）
- NL→AutoLang（§6）

### 交付
1. auto-ai: Agent::set_context（一行 + 测试）
2. auto-shell: 4 访问器 + ai_context.rs + F4/F3 接入
3. 两侧各自合并 + 推送
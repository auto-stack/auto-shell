# auto-shell (ASH) 工程审核报告

> 审核日期：2026-08-26 · 方式：静态代码审读 + 交叉验证（所有高危结论均经人工复核原码证实）
> 范围：ash-core / ash(auto-shell, ash-tui, ash) / ash-gui(ash-server, vue-handwritten, ash-gui-auto) / docs / CI
> 定位假设：ASH 是「AI 时代的结构化 Shell」，且**面向 AI Agent 作为安全执行层对外提供**——安全项按此标准评级。

---

## 一、总体评价

**做得好的地方（应保持）：**
- 分层意图清晰且基本兑现：ash-core 确实零终端依赖（无 crossterm/tokio import）；TUI 通过 render/pager/output 三个 hook 注入而非 core 反向依赖；`ash` bin 作为组合根接线。Plan 037 的架构决策落地质量高。
- 复制粘贴漂移很少：auto-shell 对 ash-core 是 re-export 共享（completions.rs 一次导出约 17 个符号），renderer 双现是生产者/消费者的有意设计。
- 测试密度高：全仓 ≈1320 个 `#[test]`，security/parser/lazy 都有密集单测；跨 shell 平价测试框架（parity.rs，支持严格模式）有设计感；examples 冒烟动态发现全部脚本。
- DEBTS.md 的债务文化是同类项目的稀有实践：每条取舍带理由与推翻条件。

**核心问题一句话版：**
1. 【最严重】作为"AI Agent 安全执行层"的安全边界不达标——存在 4 个经复核的真实漏洞/缺陷（详见 §2）。
2. `shell.rs` god object：5515 行、27 字段、205 方法、五种职责合一。
3. 工程级构建分裂：三个独立 Cargo 工程、≈6.9 GB 双 target、锁文件漂移导致 axum/windows 多版本共存编译。
4. 文档"两层皮"：内部过程文档跟进到 2026-08-26，对外用户文档停在 7 月中，且 for-agents.md 通篇描述**代码中不存在的** `ash agent` 接口。

---

## 二、安全问题（P0，全部经人工复核）

### S-1【高】ash-server 监听 0.0.0.0 且无任何鉴权 = 局域网任意命令执行
- `ash-gui/ash-server/src/bin/ash-server.rs:18-21` 绑定 `[0,0,0,0]:3000`；`http.rs:109-116` 的 `/api/run_command` 接收任意命令串并经 worker 执行；全仓 grep 无 token/auth 实现。
- 附带问题：`http.rs:40` 注释声称 "+ CORS"，实际无任何 CorsLayer（grep cors 零命中）——"声称有但没实现"；SSE 与全部端点无 Origin/Host 校验，DNS rebinding 场景下浏览器可读取 `/api/history` 并代发命令。
- **修复**：默认绑 127.0.0.1；启动令牌握手（首次打印到终端）；校验 Origin；CORS 显式配置。

### S-2【高】open_path 端点 cmd.exe 元字符注入
- `http.rs:184-195`（backend.rs:205-219 同款）将 body 中的 path 直接拼进 `cmd /C start "" <path>`，`{"path":"x & calc"}` 即可执行任意命令，构成 S-1 之外的第二攻击通道。需引号转义/参数数组传递。

### S-3【高】终端 F4/`?` AI 对话无审批门：AI 建议即执行
- CLI 入口用 `ChatSession::load()`（ash-tui/src/repl.rs:400、block_tui.rs:693），proposals=None 时所有命令注册为直接执行的 AshCommandTool（ai/mod.rs:298-311 自注 "None=全部直接执行的旧行为"），唯一闸门是 8 条子串黑名单 DANGER_PATTERNS（ash_command_tool.rs:36-45），`rm -fr /`、双空格、变量展开均可绕过。只有 GUI worker 路径有 proposal 门。
- 这与 Plan 068 的立项目标（"? 唯一 AI 入口 + 命令审批门"）直接矛盾——审批门只做了 GUI 一端。

### S-4【高】`eval_auto` + AutoLang `system()` 绕过一切工具层防护
- EvalAutoTool 对生成的 AutoLang 代码**无 danger 检查**（对比 AshCommandTool 有），且其工具描述主动鼓励 `Call system("cmd")`（ash_command_tool.rs:322-358）；ask 子命令的系统提示词直接教学 system() 用法（ai/ask.rs:39-49）。
- 另外 `ash plugin` / `ash ask` 子命令在 main.rs:71-80 提前 return，完全运行于 policy 之外。

### S-5【高】AI 线程的 Shell 不继承交互会话安全标志
- agent/chat 线程里是全新 `Shell::new()`（ash_command_tool.rs:82-83、worker.rs:291），只加载 config 文件策略；CLI 的 `--read-only`/`--sandbox` 只 set 在交互 Shell 上（main.rs:283-285）。**即使用户带着 --read-only 启动，AI 写盘畅通无阻。**
- **修复方向**：Shell 创建时强制透传交互会话 policy；eval_auto 纳入 danger 检查或禁用 system() 工具面。

### S-6【中·真 bug】单独 `--sandbox <dir>` 在交互 REPL 静默失效
- `SecurityPolicy::active()`（security.rs:85-93）不计入 sandbox_dir，而 main.rs 仅 `if policy.active()` 才 set_policy → `ash --sandbox box` 进入 REPL 后沙箱被整体丢弃。一行修复 + 补一条回归测试。

### S-7【中】proposal 门元字符绕过（GUI 端）
- 非只读命令才提案的设计下，`echo` 属只读白名单（ai/mod.rs:319-323），而参数拼装 `json_args_to_cli` 仅对含空格 token 加引号（ash_command_tool.rs:277-292）：模型传 `["hi",";","touch","pwn"]` 拼成 `echo hi ; touch pwn` 审批后直执。需要在拼接层做 shell 元字符白名单校验。

### S-8【中】read-only / no-network 是名字级黑名单，外部进程不受控
- WRITE_COMMANDS / NETWORK_EXTERNALS 按命令名精确匹配（security.rs:24-38、143-148、199-215）：`git commit`、`npm install`、`bash -c curl`、PATH 内同名二进制均放行。重定向/registry 内置命令有路径级兜底 resolve_path(for_write)（这部分是真实现了的），外部进程写入完全不管。定位应是"提醒"而非"边界"，但需在文档中如实声明。

### S-9【中】plugin install 供应链：安装即持久代码执行
- `git clone --depth 1 <url>` URL 原样来自 argv（plugin/cli.rs:344-356）；唯一校验是 plugin.at 能否解析；加载时机为 REPL 启动即 source functions.ash（loader.rs:177-187、repl.rs:78）——顶层语句立即执行；capabilities 仅 stderr 警告不强制（loader.rs:168-172，即文档宣称的权限声明实际未实现）。另 `.ashrc` 与插件加载发生在 set_policy **之前**（main.rs:281-285 先 Repl::new()）——启动期脚本不受 CLI 安全标志约束。

### S-10【低】其他
- http_* 无 SSRF 防护（file:// 读本地文件、169.254.169.254 云 metadata 可达）（http_get.rs:53-70）。
- audit 日志 Allow 记录的是执行前旧 exit_code（审计失真，shell.rs:759-764）；明文 JSONL 可篡改；可能记录密钥；默认关闭。
- F4/F3/ask 每轮把 cwd、上一条命令、部分输出发往云端 API（ai/context.rs:21-46）；聊天历史明文存盘。

---

## 三、架构问题

### A-1【严重】shell.rs god object
`ash/auto-shell/src/shell.rs`（5515 行）：`Shell` 结构体 27 字段、impl 共 205 个方法，五个职责合一——① 执行引擎（execute_* 23 个，最大函数 `execute_pipeline_with_auto` 222 行，职责含 DSL 解析/流包装/lazy 构建/渲染/赋值捕获）② 19 个**内联 builtins**（cmd_path/cmd_env/cmd_def…未走自己已有的 registry 抽象）③ 变量/glob/tilde 展开（~300 行）④ 脚本执行 ⑤ 作业控制（`cmd_suspend` 389 行）。40+ 组单测也混在同文件。
**拆分建议**：先做零风险的两刀——builtins 注册到现有 registry、expansion 抽独立模块；再考虑 execute 主链路拆 stage。每刀配合既有 685 个单测护航。

### A-2【中】三 workspace 分裂构建，锁漂移造成多版本共存
- 根目录无 workspace：ash-core（独立 crate）、ash（ws）、ash-gui/ash-server（独立 ws）各自维护 target（实测 3.0 GB + 3.9 GB）与 Cargo.lock。
- ash-server 锁文件同时存在 **axum 0.7.9 与 0.8.9**、**windows 0.58/0.61/0.62 三个版本**（windows FFI 三重编译直接撑大 target）；regex/serde/tokio 补丁版本亦漂移。
- 跨仓路径依赖锚定 `../auto-lang`、`../auto-ai` 兄弟目录布局，worktree 需要 junction hack（ash-server/Cargo.toml 注释自认）。
- **建议**：合并为一个根 workspace（cargo workspace 支持 members 跨子目录），统一 Cargo.lock 与 target；给 auto-lang/auto-ai 增加 rev-pinned 的 vendor 或 git 依赖选项作为解耦路径。

### A-3【中】前后端类型契约三处手工同步，无单一来源
- Rust 权威类型 `RenderedOutput`（ash-core/src/renderer.rs:36 起）→ ash-server/src/types.rs 手工镜像 → vue-handwritten/lib/api.ts 再手工镜像，逐字段重抄；ash-server 侧注释还指向**已删除**的 ash-gui-vue 目录，线索断裂。ash-gui-auto 侧另有 api.at IDL 但与前两者互不相通。
- **建议**：以 serde 结构体为源生成 TS 类型（specta/ts-rs/typify 任一），删除两个手抄镜像。

### A-4【中】pipeline 双数据模型过渡期拉长
- legacy `PipelineData` 与 ash-core `AtomPipeline` 并存，靠 bidirectional 桥接层慢慢迁（cmd/pipeline_convert.rs:1-4 自述）。迁移尾巴意味着每个新功能都可能要做两遍。需要给迁移定截止计划，否则桥接层固化成永久复杂度。

### A-5【中】平台分支散落 + 进程控制三套机制语义不对称
- cfg(windows)/cfg(unix) 散布 11 个文件（cp/ln/job/fs/rm/mkdir/stat/signal/convert/shell/external），无 platform 抽象层。
- 杀进程两套实现（external_stream.rs fork `/bin/kill` vs job.rs WinAPI Toolhelp+SuspendThread / libc SIGSTOP），Windows 按线程枚举挂起 vs Unix 整进程信号，语义不对称；Unix kill 依赖外部 coreutils 存在。

### A-6【中】错误处理体系几乎全是 miette 即时字符串
- 全仓 `thiserror::Error` derive 仅 1 处；错误无法编程区分类别（"命令不存在" vs "权限拒绝"）；157 处 `let _ =`/`.ok()` 吞错聚集（env 持久化写失败无声 shell.rs:3581、Mutex 中毒被忽略 suggest.rs:78 等）。
- **建议**：定义分级错误枚举（ParseError / SecurityDenial{rule} / NotFound / IoError…），至少让 agent 信封输出可以稳定编码 denial 原因。

### A-7【低】命名混淆
- ash-core/src/cmd（进程执行管道件）≠ auto-shell/src/cmd（70+ builtins）——同名异义；completions/renderer 双现虽是有意分层但缺一眼可见的说明。建议各 crate lib.rs 顶部画依赖图注释（现已有零星说明，统一格式）。

---

## 四、详细设计与实现问题

### I-1【高】外部命令 fallback 路径引号注入（正确性 bug + 注入面）
- Windows PowerShell fallback：`format!(" \"{arg}\"")` 完全不转义内嵌引号（ash-core/src/cmd/external.rs:143-150 等 3 处）；Unix fallback 只转义 `"` 不处理 `$` 与反引号（external.rs:170 等 3 处）。spawn 直接失败才会走此路径，所以平时不可见，但一旦走到：含 `" $VAR` 的参数被二次展开/截断。

### I-2【高】stdout 编码硬编码 UTF-8，中文 Windows 必乱码
- 全仓 25 处 `from_utf8_lossy`、GBK/cp936/encoding_rs 零命中（external.rs:369 等）。主开发平台是中文 Windows，`dir`/`ping` 等系统命令输出必然 lossy 丢字节。建议引入 encoding_rs 按控制台代码页解码（chcp 探测一次缓存）。

### I-3【高】子进程生命周期管理粗糙
- 捕获路径 `cmd.output()` 无限阻塞无超时（external.rs:366/424/484）；捕获模式 stdout/stderr 无上限全量进内存；流式取消是 opt-in（ExternalStream::kill），"不读 stdout 只是 orphan 进程"；每次 spawn 泄漏一个后台 wait 线程从不回收（external_stream.rs:66-71）；每 spawn 一个线程的长会话累积。DEBTS.md 已记录"阻塞路径不可取消"，可与超时一起按其建议的务实补丁推进（sleep 上限、http 默认 timeout）。

### I-4【中】host.rs 裸指针 + Mutex 重入死锁风险
- `unsafe impl Send/Sync for ShellPtr(*mut Shell)` 仅靠注释担保单线程（host.rs:22-24）；`system()` 持有 `Arc<Mutex<HostState>>` 锁期间调用 `shell.execute_capture()`（host.rs:119-138），嵌套执行若再触碰同一 state（exit_requested / 嵌套 begin_run）即非重入死锁。建议锁外调用 + debug_assert 断言。

### I-5【中】解析器"永不报错"设计
- lexer 未闭合引号静默吞到 EOF（lexer.rs:156-196）；parse_chain/parse_pipeline 返回 Vec 无 Result（parser/pipeline.rs:39/132）——不会 panic 是优点，但输错引号得到"看似成功"的错误结果，脚本调试体验差。至少对"输入耗尽但还有未闭合结构"给诊断。
- 另有两套并行 tokenizer（lexer vs external.rs parse_command），引号规则不一致，同一输入两条链路可能产生不同 argv。

### I-6【中】lazy 流水线覆盖不全 + 多处全量内存化
- 11 个 operator 只有 6 个真懒，FilterAll/FilterAny/Map/SkipBack/Reverse 出现即整链 collect（pipeline/lazy.rs:117-181），Take 短路失效；17 个内置命令（cat/sort/grep/diff/head/tail/wc…）read_to_string 全量进内存（cat.rs:36-49 把所有文件并为一个 String）。

### I-7【中】REPL 线程随意 spawn、fire-and-forget 扩散
- ai/suggest.rs:65、prompt/context.rs:121/175、completions/ai_layer.rs:185/211、code_highlight.rs:32——均 detach 后台线程 + static Mutex 结果槽，数量无上界；`sleep` 用 std::thread::sleep 直接阻塞主线程不可中断（sleep.rs:35/49）；ExternalStream 的 exit_status 用共享 Mutex 轮询而非 channel。

### I-8【低】具体地雷点（重构时注意）
- shell.rs:4316 cmd_bg 中 resume_job 与 get_mut(id).unwrap() 两步之间无原子性；shell.rs:3496 脚本行循环吞错继续跑；lexer Scanner::byte() 无检查索引（与多字节字符边界假设强耦合）；tokens_to_string quote 风格丢失导致历史回显失真。

---

## 五、测试与 CI

| 事实 | 证据 |
|---|---|
| 总量 ≈1320 测试（单元 1230 / 集成 90，集成占比仅 7%） | grep 计数 |
| ash-tui 138 个单测**不在 CI** | ci.yml 仅 `-p ash-core` 与 `-p auto-shell` |
| examples_smoke/parity 两套 e2e **不在 CI**——"30+ 脚本回归网"实际只在本地生效 | ci.yml:59-69 |
| ash-server **0 测试**；vue-handwritten 无 package.json/测试 | 目录检查 |
| CLI 安全旗标（parse_security_flags）无专门集成安全套件，security 24 测试全在单元层 | ash-core/src/security.rs |
| clippy 两 crate 均 continue-on-error 不阻塞；无 rustfmt job；ci.yml:79 parity 测试选择器指向 `ash` 包改名前的位置疑似失效 | ci.yml:104-108 |
| 无 rustfmt.toml/clippy.toml/rust-toolchain.toml，Rust 版本仅 README 徽章声明 | 仓库根 |

**优先动作**：把 CI 改为对整个 `ash` workspace 跑 `cargo test --workspace`（一并修复 parity 选择器）；ash-server 至少补 http 层路由测试；新增安全集成套件（S-3~S-7 每条对应一个红→绿测试）。

---

## 六、文档现状（"两层皮"）

**新的一层（健康）**：docs/plans/archive（至 08-26）、old（52 篇历史）、DEBTS.md、TODO.md、designs/15 篇。
**旧的一层（过期）**：README/quickstart/for-developers/for-agents/roadmap 停在 07 月中下旬；release_notes 停在 v0.5（两个月空白）。

**过期/矛盾具体条目：**
1.【最重】`ash agent describe-tools/check/run` 整个子命令族**在代码中不存在**（.rs 全仓零命中），而 README/for-agents.md 通篇以其为基础构建，包括 "`tool_count: 79`" 这个无代码锚点的数字（实际命令模块 80 个）。这是对新集成的 Agent 开发者的当面误导。
2. F3 NL→command 已于 Plan 069 退役（commit 2d53032），README:66-70/:174、quickstart §5、for-developers F1-F4 表仍在教。
3. SmartCommand 表面已删（Plan 066/067），README 仍列"设计中"特性表。
4. README 项目结构图把 auto-shell/ash 的角色写反（auto-shell 是纯逻辑 crate，ash 才是二进制），漏列 ash-tui；docs/plans 描述与现状不符。
5. types.rs 注释指向已删除的 ash-gui-vue。

---

## 七、是否需要反向生成一整套文档？

**结论：有必要补文档体系，但不建议按"需求总结 + 架构设计 + 详细设计 + 测试设计"四件套一次性大而全反生成——那会生产出一批立刻开始腐烂的纸。本项目已有的病灶恰恰是"文档说了谎"（§六），反向生成如果不解决"防腐烂机制"，只是复制问题。**

推荐做法（按性价比排序）：

1. **先止血（半天）**：修正说谎的文档——README/for-agents/SKILL 与代码对齐。`ash agent` 要么实现要么从文档撤下；F3 相关内容删除。
2. **生成 5 份小而准的活文档（1~2 天，可用 AI 辅助，本报告第三、五章即是素材）**：
   - `ARCHITECTURE.md`：crate 依赖图 + 请求数据流（REPL/GUI 两条链）+ 模块职责一览（A-7 的命名澄清一并解决）。
   - **安全模型一页纸（THREAT-MODEL.md）**：明确哪些是边界（sandbox 路径限制、审计）、哪些只是提醒（no-network 名单、DANGER_PATTERNS）、哪些根本没生效（CLI 审批门、插件 capabilities）。这份文档与 §二的修复联动，是对外承诺的基础。
   - **API/协议契约单一来源**：serde 结构体 → TS typegen（代码生成代替手抄，属 A-3 架构修复的一部分，"文档"自动不过期）。
   - **ADR/决策索引**：你们其实已经有 ADR 的素材——docs/plans/archive 每份归档计划都是一份决策记录，补一个 `docs/adrs/INDEX.md` 按"决策—状态—取代关系"编排即可，不必重写。
   - **TESTING.md（测试地图)**：1320 个测试怎么组织、缺口在哪（引用本报告 §五）。
3. **需求文档**：不建议机器反推（代码推不出"为什么"）。建议由人从 TODO.md + roadmap.md + plans/archive 提炼一份 2~3 页的产品定位文档（三类用户 × 各自入口 × 当前能力边界），半天工作量。
4. **防腐机制（关键，无此项则回到现状）**：
   - CI 增加"文档冒烟"：从 README/for-agents/SKILL.md 提取的 `ash xxx` 示例逐一对照子命令是否存在（一个百行脚本即可）；
   - release_notes 跟随 tag 流水线自动生成草稿（已具备 release.yml 基础）；
   - 约定：改公共行为（子命令/键位/工具清单）的 PR 必须触及对应文档，review checklist 固化。

---

## 八、优化路线图建议

**P0（本周，安全止血 + 文档止血）**
1. S-1/S-2：ash-server 默认绑 127.0.0.1 + 启动 token + Origin 校验；open_path 参数数组化。
2. S-3/S-4/S-5：CLI chat 接入 proposal 门（复用 GUI 的 ChatSession::with_client_and_proposals 已有能力）；eval_auto 加 danger 检查；AI 线程 Shell 强制透传交互 policy。
3. S-6：`active()` 计入 sandbox_dir（一行 + 回归测试）。
4. CI：`cargo test --workspace` 化（覆盖 ash-tui/examples e2e），修复 parity 选择器，rustfmt/clippy 转硬门槛。
5. 文档止血：README/for-agents/SKILL 删虚构接口与 F3 内容。

**P1（两周内，结构与契约）**
6. 合并根 workspace：统一 Cargo.lock/target，消 axum/windows 多版本。
7. shell.rs 第一刀拆分：内联 builtins 迁入 registry（调用点不动），expansion 抽模块。
8. 类型契约 codegen（Rust→TS），删除两处手工镜像。
9. fallback 引号转义修复（I-1）+ 中文控制台编码层（I-2）。
10. 外部命令默认超时 + sleep/http 务实上限（与 DEBTS.md 既定方案一致）。

**P2（一个月，偿还内核债）**
11. PipelineData → AtomPipeline 收尾时间表（A-4），桥接层限期拆除。
12. 错误类型分域（thiserror 枚举），denial 可编程识别（对 agent 场景尤其重要）。
13. 解析器诊断化（未闭合结构报错），tokenizer 二合一。
14. lazy operator 全覆盖 + 大文件流式（cat/grep/head 分块）。
15. 安全集成测试套件（本文档 §二每条一个用例）。

**P3（季度）**
16. 平台抽象层收敛（进程控制三套合一）、GUI(ash-server) 测试从 0 到 1、前端测试决策（vitest 或依赖 .at 侧校验二选一）。

---
*方法说明：本报告由 4 路并行深查（架构/引擎实现/安全/测试与文档）汇总，标【高】及以上结论均经原作者交叉核验原码行号后确认。*

# Rust build artifacts — each workspace (ash/, ash-gui/...) has its own target/
/target/
**/target/

# Binaries / build output
*.exe
*.pdb

# Editor / OS cruft
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Note: Cargo.lock IS committed — these are binary crates (ash, ash-gui),
# where lockfiles should be tracked.
# Exception: ash-core/Cargo.lock is NOT tracked — ash-core is consumed as
# a path dependency (a library), so the lockfile is a build-time artifact.
/ash-core/Cargo.lock

# Local scratch/scratch files for manual testing
**/tmp/

# auto-man / AutoCache runtime state (per-project, regenerated on codegen)
**/.am/

# auto-lang codegen output for ash-gui-auto (regenerated by `auto gen`/
# `auto build`; 015-notes commits its gen/, but here these are verification
# artifacts for Plan 043 M5 — regenerate before use, don't commit).
ash-gui/ash-gui-auto/gen/
# `auto build` also emits a bundled frontend (dist/) and a generated Rust
# workspace scaffold (examples/rust-workspace) — both are build artifacts.
ash-gui/ash-gui-auto/dist/
ash-gui/ash-gui-auto/examples/

# Git worktrees (isolated workspaces for feature branches)
/.worktrees/

# ZCode session cache (per-session plan artifacts — not project content)
/.zcode/
__pycache__/
*.pyc
.pytest_cache/
*.auto/

# 调试产物(会话性日志/截图/临时工程,不入库)
/dbg-*.log
/vm-out.log
/ic2-*.log
/ic2-hdr.png
/hdr-*.png
/snap-bounds.txt
/icontest2/
/vt.json

# Windows recycle-bin artifact that occasionally appears at repo root (system-protected, undeletable without admin)
$RECYCLE.BIN/
